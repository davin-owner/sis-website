# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files - only ignore ones with secrets
.env*.local       # All .local files (have secrets)
.env              # Base .env (if it exists)

# .env.development and .env.production are safe to commit (no secrets)
# They contain only public config and are committed to git

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
